{
  "name": "synapse",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Synapse — personal automation & AI playground, shipped as a Manifest V3 browser extension.",
  "scripts": {
    "dev": "npm run dev:browser",
    "dev:browser": "vite",
    "build": "npm run build:browser",
    "build:browser": "vite build",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@picocss/pico": "^2.1.1",
    "vanjs-core": "^1.5.3"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.0.0-beta.31",
    "@types/chrome": "^0.0.287",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
